#!/usr/bin/env bash

ldapsearch -x -H {{ sshd_ldap_servers }} -b '{{ sshd_ldap_basedn }}' '(&(objectClass=ldapPublicKey)(uid='"$1"'))' 'sshPublicKey' | sed -n '/^ /{H;d};/sshPublicKey:/x;$g;s/\n *//g;s/sshPublicKey: //gp'
